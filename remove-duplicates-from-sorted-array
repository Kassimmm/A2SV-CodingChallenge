class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        i = 0
        output = []

        while i < len(nums):
            if nums[i] in output:
                pass
                i += 1
            else:
                output += [nums[i]]

        nums[:] = output
